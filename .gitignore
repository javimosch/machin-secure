# build artifacts
secure
secure.mfl

# scratch reports / verdict stores created while testing locally
# (scope *.html to repo root so docs/*.html is tracked)
/*.html
.machin-secure.verdicts.json
test/fixtures/.machin-secure.verdicts.json
machin-secure.sarif

# os
.DS_Store
