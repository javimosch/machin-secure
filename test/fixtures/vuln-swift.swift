import WebKit
import Foundation
import Security

let val = optional!
webView.evaluateJavaScript(req.body)
UserDefaults.standard.set(password, forKey: "password")
let url = "http://api.example.com/v1"
let session = URLSession(configuration: config, delegate: nil, delegateQueue: nil)
db.executeQuery("SELECT * FROM users WHERE id=" + id)
let accessible = kSecAttrAccessibleAlways
print("token: \(token)")
let key = apiKey = "AKIAIOSFODNN7EXAMPLE123"
let cred = UserDefaults.standard.string(forKey: "secret_token")
let result = try! fetch()
let ats = NSAllowsArbitraryLoads
let kc = accessibleWhenUnlocked
