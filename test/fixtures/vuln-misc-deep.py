import os, tempfile
os.system("echo " + req.body)
open(req.query.file)
os.remove(req.query.file)
with open(req.query.file, "w") as f: f.write("data")
print("10.0.0.1")
# TODO: fix this
printf(req.body)
tmpfile = "/tmp/data"
LIKE '%req.body%'
ORDER BY req.query.sort
LIMIT req.query.limit
OFFSET req.query.offset
LIKE '% + req.body + %'
