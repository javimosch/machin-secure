counter += 1;
new Array(10000000);
while (true) {}
fetch("https://api.example.com");
const timeout = 0;
Promise.all(items.map(item => fetch(item)));
const cache = { size: 0 };
const queue = { maxSize: 0 };
const pool = { maxPoolSize: 500 };
const socket = new Socket();
const stream = Readable();
app.listen(3000);
MAX_UPLOAD_SIZE
bodyParser limit
rateLimit global
timeout = 9999999
