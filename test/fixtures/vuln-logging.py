import logging
logging.info(f"password: {password}")
logging.info(f"secret: {secret}")
logging.info(f"token: {token}")
logging.info(f"credit card: {cc}")
logging.info(f"ssn: {ssn}")
traceback.print_exc()
try:
    pass
except:
    pass
logging.debug(req.body)
LOG_LEVEL = "DEBUG"
